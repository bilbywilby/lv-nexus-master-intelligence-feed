<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LV-Nexus SSR Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          fontFamily: {
            mono: ['JetBrains Mono', 'Fira Code', 'Consolas', 'monospace']
          },
          colors: {
            slate: { 950: '#020617' },
            amber: { 400: '#fbbf24' },
            emerald: { 400: '#34d399' },
          },
          boxShadow: {
            glow: '0 0 20px -5px rgba(251, 191, 36, 0.4)',
          }
        }
      }
    }
  </script>
  <style>
    body { background-color: #020617; color: #e2e8f0; }
    .glass { background-color: rgba(2, 6, 23, 0.5); backdrop-filter: blur(10px); border: 1px solid rgba(51, 65, 85, 0.5); }
    .severity-Critical { border-color: #ef4444; color: #f87171; }
    .severity-High { border-color: #f97316; color: #fb923c; }
    .severity-Medium { border-color: #f59e0b; color: #fbbf24; }
    .severity-Low { border-color: #eab308; color: #fde047; }
    .severity-Info { border-color: #0ea5e9; color: #38bdf8; }
  </style>
</head>
<body class="font-mono">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <h1 class="text-3xl font-bold text-amber-400 tracking-wider mb-8">LV-NEXUS DASHBOARD (SSR)</h1>
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      <div class="lg:col-span-2 glass rounded-lg p-6">
        <h2 class="text-xl font-bold text-emerald-400 mb-4">Live Feed</h2>
        <div class="overflow-x-auto">
          <table class="w-full text-sm text-left">
            <thead class="text-xs text-slate-400 uppercase">
              <tr>
                <th scope="col" class="py-3 px-6">Severity</th>
                <th scope="col" class="py-3 px-6">Title</th>
                <th scope="col" class="py-3 px-6">Location</th>
              </tr>
            </thead>
            <tbody>
              <% items.forEach(function(item) { %>
                <tr class="border-b border-slate-700">
                  <td class="py-4 px-6">
                    <span class="px-2 py-1 rounded-full text-xs font-semibold border <%= 'severity-' + item.severity %>"><%= item.severity %></span>
                  </td>
                  <td class="py-4 px-6 font-medium"><%= item.title %></td>
                  <td class="py-4 px-6 text-slate-400"><%= item.location %></td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      </div>
      <div class="space-y-8">
        <div class="glass rounded-lg p-6">
          <h3 class="text-sm font-medium text-slate-300">Critical Alerts</h3>
          <p class="text-2xl font-bold text-amber-400"><%= stats.critical %></p>
        </div>
        <div class="glass rounded-lg p-6">
          <h3 class="text-sm font-medium text-slate-300">Total Events</h3>
          <p class="text-2xl font-bold text-amber-400"><%= stats.total %></p>
        </div>
        <div class="glass rounded-lg p-6">
          <h3 class="text-sm font-medium text-slate-300">Event Velocity</h3>
          <p class="text-xs text-slate-500">(Mock Sparkline)</p>
          <div class="h-16 bg-slate-800/50 rounded mt-2"></div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>